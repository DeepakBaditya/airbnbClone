<% layout("/layouts/boilerplate") %>

<body>
    <!-- <form action="/listings/new" method="get">
        <button style="color: #fff;background-color: #000;border-radius: 12px;padding: 0 auto;height: 30px;width: 90px;">New listing</button>
    </form> -->
    <h3>All Listings</h3>
    <div class="row row-cols-lg-3 row-cols-md-2 row-cols-sm-1">
        <% for(let listing of allListings){ %>
            <a href="/listings/<%=listing._id%>" class="text-decoration-none">              
                <div class="card col" style="width: 18rem;">
                    <img src="<%=listing.image%>" class="card-img-top" alt="listing image" style="height: 20rem;">
                    <div class="card-img-overlay"></div>
                    <div class="card-body">
                        <p class="card-text">
                            <b><%= listing.title %></b> <br>
                            </b>&#X20B9;<%= listing.price.toLocaleString("en-IN") %> /night<br>
                            <%= listing.location %>
                        </p>
                    </div>
                </div>
            </a>
        <% } %>
    </div>
    <!-- <ul>
        <% for(let listing of allListings){ %>
            <li>
                <a href="/listings/<%=listing._id%>"><%=listing.title%></a>
            </li>
        <% } %>
    </ul> -->
</body>
</html>
